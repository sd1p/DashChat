// Tailwind CSS v4 — the entire framework is a single PostCSS plugin; there is
// no tailwind.config.js (theme + content are configured in app/globals.css).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
