# ----------------------------------------------------------------------------
# DashChat backend environment configuration
# Copy this file to ".env" (in this same backend/config directory) and fill in
# real values. server.js loads it via: dotenv.config({ path: "backend/config/.env" })
# ----------------------------------------------------------------------------

# Port the Express/Socket.IO server listens on
PORT=5000

# Node environment: "development" for local, "production" when serving built frontend
NODE_ENV=development

# MongoDB connection string (stores users, chats, and messages).
# Local example: mongodb://127.0.0.1:27017/dashchat
# Atlas example: mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/dashchat
MONGODB_URI=mongodb://127.0.0.1:27017/dashchat

# --- Clerk authentication ---
# Get these from the Clerk dashboard: https://dashboard.clerk.com (API Keys).
# The secret key must stay server-side only — never expose it to the frontend.
CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
# Your frontend origin (used by Clerk for allowed origins / dev).
CLERK_FRONTEND_URL=http://localhost:5173
